# ws_server

<!-- claude-memory-pointer -->
> **Project memory cache:** `~/.claude/projects/-home-opt-Claude-ws-server/`
> Auto-memory + session transcripts are path-keyed by this folder’s absolute path.
> If this folder was moved/renamed and memory looks empty, load it from the path above
> (and `mv` that dir to the new path-key to restore auto-loading). See rule [[project-memory-pointer]].
<!-- /claude-memory-pointer -->
